// Re-export types and enums from shared package
export {
  EventType,
} from '@uniconnect/shared';

export type {
  Event,
  CreateEventPayload,
  UpdateEventPayload,
  EventFilters,
  PaginationParams,
  FENResponse,
  ErrorDetails,
  PaginationMetadata,
} from '@uniconnect/shared';
