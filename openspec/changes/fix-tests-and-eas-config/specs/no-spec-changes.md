## No Spec Changes

This change is a bugfix and configuration adjustment. No new capabilities are introduced, and no existing capability requirements are modified.

### Referenced Existing Specs

- `test-infrastructure`: Test fixes for EventsService and MessagesService specs.
- `real-time-message-observation`: Fix to PrivateChatObserver event name and CoR mentions handler validation logic.
- No spec-level requirement changes apply.
